const labelPrefix = '@project';

export const setupProjectLabel = `${labelPrefix}/SETUP_PROJECT`;

export const loadingProjectLabel = `${labelPrefix}/IS_LOADING_PROJECT`;

export const progressLoadingProjectLabel = `${labelPrefix}/PROGRESS_LOADING_PROJECT`;

export const addCardInListLabel = `${labelPrefix}/ADD_CARD_IN_LIST`;

export const addListInBoardLabel = `${labelPrefix}/ADD_LIST_IN_BOARD`;
